{%if posts is defined %}
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Post Title</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        
                        {% set foo = 1 %}
                        {% for post in posts %}
                        <tr>
                            <td><h5><strong>{{ foo }}</strong></h5></td>
                            <td><h5>{{ post.title }}</h5></td>
                            <td class="text-right">
                                <a href="{{ path('edit_post', {'id': post.id}) }}" class="btn btn-success btn-sm"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span></a>
                                <a href="{{ path('view_post', {'id': post.id}) }}" class="btn btn-primary btn-sm"><span class="glyphicon glyphicon-search" aria-hidden="true"></span></a>
                                <a href="{{ path('delete_post', {'id': post.id}) }}" class="btn btn-danger btn-sm"><span class="glyphicon glyphicon-trash" aria-hidden="true"></span></a>
                            </td>
                        </tr>
                        {% set foo = foo + 1 %}
                        {% endfor %}
                        
                </table>
            {% else %}
                <p>You have no posts published. Start adding new.</p>
            {% endif %}